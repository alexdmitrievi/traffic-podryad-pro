{
  "name": "@traffic/backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "echo '@traffic/backend: no TypeScript sources yet (Wave 4)'",
    "build": "echo '@traffic/backend: nothing to build yet (Wave 4)'",
    "test": "bun test prisma",
    "prisma": "prisma",
    "prisma:validate": "prisma validate",
    "prisma:format": "prisma format",
    "prisma:generate": "prisma generate"
  },
  "devDependencies": {
    "prisma": "^7.9.1"
  }
}
